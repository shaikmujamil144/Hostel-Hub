{
  "name": "hostelhub",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,green \"npm run dev --workspace hostelhub-api\" \"npm run dev --workspace web\"",
    "dev:api": "npm run dev --workspace hostelhub-api",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace shared && npm run build --workspace db && npm run build --workspace hostelhub-api && npm run build --workspace web",
    "build:api": "npm run build --workspace hostelhub-api",
    "build:web": "npm run build --workspace web",
    "start": "npm run start --workspace hostelhub-api",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.5.6"
  }
}